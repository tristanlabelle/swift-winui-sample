// Generated by swift-winrt
// swiftlint:disable all

import WindowsRuntime
import WinUI_ABI
import UWP
import WindowsAppSDK


/// Provides event data for the ProcessKeyboardAccelerators event.
public final class MicrosoftUIXamlInput_ProcessKeyboardAcceleratorEventArgs: WindowsRuntime.WinRTImport<MicrosoftUIXamlInput_ProcessKeyboardAcceleratorEventArgsBinding>, @unchecked Sendable {
    /// true to mark the event handled. false to leave the event unhandled. The default is false.
    public var handled: Swift.Bool {
        get throws {
            try _interop.get_Handled()
        }
    }

    /// Sets a value that marks the event as handled.
    /// - Parameter newValue: true to mark the event handled. false to leave the event unhandled. The default is false.
    public func handled(_ value: Swift.Bool) throws {
        try _interop.put_Handled(value)
    }

    /// Gets or sets a value that marks the event as handled.
    public var handled_: Swift.Bool {
        get {
            try! self.handled
        }
        set {
            try! self.handled(newValue)
        }
    }

    /// The virtual-key.
    public var key: WindowsSystem_VirtualKey {
        get throws {
            try _interop.get_Key()
        }
    }

    /// Gets the virtual-key (used in conjunction with one or more modifier keys) for a keyboard shortcut (accelerator).

    /// A keyboard shortcut is invoked when the modifier keys associated with the shortcut are pressed and then the non-modifier key is pressed at the same time. For example, Ctrl+C for copy and Ctrl+S for save.
    public var key_: WindowsSystem_VirtualKey {
        try! self.key
    }

    /// The virtual-key.
    public var modifiers: WindowsSystem_VirtualKeyModifiers {
        get throws {
            try _interop.get_Modifiers()
        }
    }

    /// Gets the virtual-key used to modify another keypress for a keyboard shortcut (accelerator).

    /// A keyboard shortcut is invoked when the modifier keys associated with the shortcut are pressed and then the non-modifier key is pressed at the same time. For example, Ctrl+C for copy and Ctrl+S for save.
    public var modifiers_: WindowsSystem_VirtualKeyModifiers {
        try! self.modifiers
    }

    // MARK: Implementation details

    public required init(_wrapping inner: consuming COM.COMReference<SWRT_MicrosoftUIXamlInput_IProcessKeyboardAcceleratorEventArgs>) {
        super.init(_wrapping: consume inner)
    }

    private static var _lazyIActivationFactory: COM.COMReference<SWRT_IActivationFactory>.Optional = .init()

    private static var _iactivationFactory: COM.COMInterop<SWRT_IActivationFactory> {
        get throws {
            try _lazyIActivationFactory.lazyInitInterop {
                try WindowsRuntime.activationFactoryResolver.resolve(runtimeClass: "Microsoft.UI.Xaml.Input.ProcessKeyboardAcceleratorEventArgs")
            }
        }
    }
}